<head>
  <style>

  .card{
  padding: 10px;
  margin: 15px;
  height: inline-block;
  box-shadow: 0px 0px 3px 2px green;
  width: fit-content;
  display: inline-block;
  /* float:left; */
  }

  p{
  line-height: 20px;
  }
  .btn-field{
  display: flex;
  }

  </style>
</head>  
<body>
  <div class="wrap">
    <h3>Booking</h3>
    <% @ud.each do |user| %>
      
        <% if  current_user.admin || user.name == current_user.name %>
      <div class="card">
          <p><strong>Name: </strong><%= user.name %></p>
          <p><strong>Car Number: </strong><%= user.car_number %></p>
          <p><strong>Car Color: </strong><%= user.car_color %></p>
          <p><strong>In Time: </strong><%= user.in_time %></p>
          <p><strong>Out Time: </strong><%= user.out_time %></p>
          <p><strong>Slot Id: </strong><%= user.slot_id%></p>
           <p><strong>Floor Id: </strong><%= user.slot.floor_id%></p>
          <button><%= link_to 'Show', user_detail_path(user) %></button>

          <% if  current_user.admin %>
              <%= link_to 'Delete', user_detail_path(user),data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>|
              <%= link_to 'Edit', edit_user_detail_path(user) %>
          <% end %>
        <% end %>
      </div>
    <% end %>
    <button><%= link_to 'back', root_path %></button>
  </div>
</body>


























<%
=begin%>
 <p>
  <%= link_to 'Create new users ', new_user_path %></br>
  <%= link_to 'return to user prog', users_path %>
</p> 
<%
=end%>
   <%
=begin%>
 <td><%= user.floor_id %></td 
<%
=end%>
