
<h3> All Floor</h3>

<head>
  <style>
  .card{
  padding: 10px;
  margin: 15px;
  height: inline-block;
  box-shadow: 0px 0px 3px 2px green;
  width: fit-content;
  display: inline-block;
}

p{
  line-height: 20px;
}
.btn-field{
  display: flex;
}
  </style>
</head>  
<body>
    <div class="wrap">
      <% @floor.each do |floor| %>
        <div class="card">
            <p><strong>Floor Number: </strong><%= floor.number_of_floor %></p>
            <p><strong>Floor Id: </strong><%= floor.id %></p> 
            <%= "This floor has #{floor.slots.count} slots"%></td> 
            <button><%= link_to 'Show', floor_path(floor) %></button></td>
        </div>
      <% end %>
      <button><%= link_to 'back', root_path %></button>
      <% if current_user.admin%>
      <button><%= link_to 'Add Floor', new_floor_path %></button>
      <%end%>
    </div>
  
</body>









<%
=begin%>
 <% @user_detail.floor.each do |car|%>
            <li> <%= link_to 'apply for booking', new_user_detail_path(car)%> </li>
            <%end%> 
<%
=end%>





          <%
=begin%>
 <td><%= link_to 'Edit', edit_floor_path(floor) %></td>   
<%
=end%>
    

<%
=begin%>

<ul>
# <li><%= link_to 'create floor', new_floor_path %></li>
</ul>
 

  <td><%= link_to 'Delete', floor_path(floor),data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %></td>
          <td><%= link_to 'Edit', edit_floor_path(floor) %></td>  
<%
=end%>
